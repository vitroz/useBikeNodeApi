const { Pool } = require('pg')

//var connect = "postgres://vitor:root@localhost/usebikemobile";

const pool = new Pool({
  user: 'vitor',
  host: 'localhost',
  database: 'usebikemobile',
  password: 'root',
  port: 5432,
});

module.exports = {
  query: (text, params, callback) => {
    return pool.query(text, params, callback)
  }
}